<%= form_for @department, html: { multipart: true } do |f| %>
	<%= f.label :name %><%= f.text_field :name %><br>
	<%= f.label :location %> <%= f.text_field :location %><br>
	<%= f.submit 'Submit' %>
<% end %>